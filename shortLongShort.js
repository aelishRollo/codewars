function solution(a, b){
    if (a.length < b. length) {
      return a+b+a
    } else {
      return b+a+b
    }
  }
  
  /*
  Params: a and b which are both strings
  
  Return: shorter string + longer string + shorter string, all concatenated
  
  */
