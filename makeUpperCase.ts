export function makeUpperCase(str: string): string{
    return str.toUpperCase();
  }
  
  
  //PREP
  
  //Parameters: a string
  
  //Return value: an uppercase verson of the string
  
  //Example: 'bonnaroo' => 'BONNAROO'
  
  //Psuedocode: return str.toUpperCase()
